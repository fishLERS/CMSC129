import React from 'react';

export default function AdminHome() {
	return (
		<div className="p-6">
			<h1 className="text-2xl font-semibold">Admin Dashboard</h1>
			<p className="mt-2 text-sm">Welcome, admin — manage users and site settings here.</p>
		</div>
	);
}
